spring.datasource.driverClassName: com.mysql.jdbc.Driver
spring.datasource.url: jdbc:mysql://127.0.0.1:3306/zhgd?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
spring.datasource.username: root
spring.datasource.password: 580926


jpa:
    hibernate:
    #create根据定义的类自动生成表格（每次重启便会删除前次的表）
    #除了create，常用的是update，第一次也会生成，但之后操作不会删表
        ddl-auto:update
        #在控制台看到sql语句，方便调试
        show-sql:true

